import {FC} from "react";
import {IconButton} from "./icon-button";

//TODO メニューボタン追加
export const TopMenu: FC = ()=> {

    return (
        <div id={"top"}>
            <IconButton id={"test"} onClick={()=>{}}/>
        </div>
    );
}
