import {Job} from "./job";
import {TagModel} from "../tag/tag-model";

export  class QiitaArticleJob extends Job{
    private latestArticle: TagModel;
    notify(): void {

    }

    runMethod(): boolean {
       const client = new ;
    }

}
